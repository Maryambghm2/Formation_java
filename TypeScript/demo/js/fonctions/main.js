// Les fonction ts son définie 
// Avec l'ajout des types pour les params et le retour
function add(a, b) {
    return a + b;
}
let result = add(2, 3);
//Paramètres avec valeur par défaut
function add2(a, b = 15) {
    return a + b;
}
// fonction fléchée 
let maFonction = (x, y) => {
    return x + y;
};
// Fonction générique 
function test(arg) {
    return arg;
}
